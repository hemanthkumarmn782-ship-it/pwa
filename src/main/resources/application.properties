# ==============================
# Database Configuration
# ==============================
# application.properties (The file on your GitHub repo)

# application.properties

# 1. Driver must be PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver


spring.datasource.url=jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres?prepareThreshold=0

spring.datasource.username=postgres.fvivcucrapgyegvyrufw
spring.datasource.password=hemanth@200


# 5. Recommended: HikariCP Timeout Setting
# Give the connection pooler some time to establish a session.
spring.datasource.hikari.connection-timeout=30000

# ... other settings as before ...

# ==============================
# Hibernate & JPA Configuration
# ==============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


# ==============================
# Server Configuration
# ==============================
server.port=8080
